sample:
  name: GATT Generated {{ profile.name }}

tests:
  sample.bluetooth.ble_gatt_generator_{{ profile.name }}:
    build_only: true
    platform_allow:
      - native_sim
      - nrf52_bsim/native
    tags: bluetooth
