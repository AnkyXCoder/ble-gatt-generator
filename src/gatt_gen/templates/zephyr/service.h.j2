/*
 * Copyright (c) {{ year }} Ankit Modi
 *
 * SPDX-License-Identifier: Apache-2.0
 */

/** @file
 *  @brief {{ profile.name }} GATT service definition.
 *
 *  Auto-generated by gatt-gen; do not edit.
 */

#ifndef {{ service.name | c_guard }}
#define {{ service.name | c_guard }}

#include <stdint.h>
#include <zephyr/bluetooth/gatt.h>

{% for chrc in service.characteristics %}
/** @brief Set the {{ chrc.name }} characteristic value.
 *
 *  @param data Pointer to the data to copy into the value buffer.
 *  @param len  Number of bytes to copy; must not exceed the value size.
 *
 *  @return 0 on success, or a negative error code on failure.
 */
int {{ service.name }}_{{ chrc.name }}_set(const uint8_t *data, uint16_t len);

/** @brief Get the {{ chrc.name }} characteristic value.
 *
 *  @param data Buffer to receive the value.
 *  @param len  Number of bytes to copy; must not exceed the value size.
 *
 *  @return 0 on success, or a negative error code on failure.
 */
int {{ service.name }}_{{ chrc.name }}_get(uint8_t *data, uint16_t len);
{% if chrc.has_notify() %}
/** @brief Notify the {{ chrc.name }} characteristic.
 *
 *  @param data Pointer to the data to update and notify.
 *  @param len  Length of the data in bytes.
 *
 *  @return 0 on success, or a negative error code on failure.
 */
int {{ service.name }}_{{ chrc.name }}_notify(const uint8_t *data, uint16_t len);
{% endif %}
{% if chrc.has_indicate() %}
/** @brief Indicate the {{ chrc.name }} characteristic.
 *
 *  @param data Pointer to the data to update and indicate.
 *  @param len  Length of the data in bytes.
 *
 *  @return 0 on success, or a negative error code on failure.
 */
int {{ service.name }}_{{ chrc.name }}_indicate(const uint8_t *data, uint16_t len);
{% endif %}
{% endfor %}

#endif /* {{ service.name | c_guard }} */
